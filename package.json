{
  "dependencies": {
    "esbuild": "^0.17.8",
    "purescript": "^0.15.9",
    "purescript-psa": "^0.8.2",
    "spago": "^0.20.8"
  },
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "spago build --purs-args '--censor-lib --strict'",
    "example-counter2": "spago bundle-app -m Example.Counter2 --to examples/dist/bundle-counter2.js"
  }
}
